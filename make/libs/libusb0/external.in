config EXTERNAL_FREETZ_LIB_libusb0
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libusb0
	bool "libusb-0.1"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libusb-0.1.so.4.4.4

