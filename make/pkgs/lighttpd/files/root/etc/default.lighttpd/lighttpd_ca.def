CAPTION="Lighttpd: $(lang de:"CA Zertifikat" en:"CA certificate")"
DESCRIPTION="$(lang de:"CA Zertifikat in PEM Format hier einf&uuml;gen, falls das lighttpd Zertifikat signiert wurde. Ansonsten diese Box leer lassen." en:"Add the CA certificate in PEM format if your lighttpd certificate was signed. Otherwise leave the box empty.")"

CONFIG_FILE='/tmp/flash/lighttpd/ca.pem'
CONFIG_SAVE='modsave flash; pidof lighttpd >/dev/null && /mod/etc/init.d/rc.lighttpd restart'
CONFIG_TYPE='text'

