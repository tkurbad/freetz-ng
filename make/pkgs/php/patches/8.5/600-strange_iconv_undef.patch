--- ext/iconv/iconv.c
+++ ext/iconv/iconv.c
@@ -40,7 +40,7 @@
 #endif
 
 #ifdef HAVE_LIBICONV
-#undef iconv
+/*#undef iconv*/
 #endif
 
 /* iconv can have different constiness for src on some platforms;
